import { Vector2 } from "./Objects/Vector2";
import { PathControlPoint } from "./PathControlPoint";
export declare class SliderPath {
    Version: number;
    private expectedDistance;
    get ExpectedDistance(): number;
    set ExpectedDistance(value: number);
    get HasValidLength(): boolean;
    private controlPoints;
    get ControlPoints(): PathControlPoint[];
    set ControlPoints(value: PathControlPoint[]);
    private readonly calculatedPath;
    private readonly cumulativeLength;
    private readonly pathCache;
    private optimisedLength;
    private calculatedLength;
    private readonly segmentEnds;
    private segmentEndDistances;
    constructor(sliderPath: Partial<SliderPath>);
    get Distance(): number;
    get CalculatedDistance(): number;
    private optimiseCatmull;
    get OptimiseCatmull(): boolean;
    set OptimiseCatmull(value: boolean);
    GetPathToProgress(path: Vector2[], p0: number, p1: number): void;
    PositionAt(progress: number): Vector2;
    PointsInSegment(controlPoint: PathControlPoint): PathControlPoint[];
    GetSegmentEnds(): number[];
    invalidate(): void;
    private ensureValid;
    private calculatePath;
    private calculateSubPath;
    private calculateLength;
    MirrorPath(): void;
    private progressToDistance;
    private interpolateVertices;
    private indexOfDistance;
}
