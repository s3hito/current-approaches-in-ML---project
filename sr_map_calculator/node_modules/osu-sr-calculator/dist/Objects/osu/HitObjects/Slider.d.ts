import { Vector2 } from "../../Vector2";
import { SliderTailCircle } from "./SliderObjects/SliderTailCircle";
import { OsuHitObject } from "./OsuHitObject";
import { SliderHeadCircle } from "./SliderObjects/SliderHeadCircle";
import { Beatmap } from "../Beatmap";
import { SliderPath } from "../../../SliderPath";
import { SliderRepeat } from "./SliderObjects/SliderRepeat";
import { TypedCache } from "../../Cache";
import { HitWindows } from "../HitWindows";
export declare class Slider extends OsuHitObject {
    get EndTime(): number;
    get Duration(): number;
    readonly endPositionCache: TypedCache<Vector2>;
    get EndPosition(): Vector2;
    StackedPositionAt(t: number): Vector2;
    private readonly path;
    get Path(): SliderPath;
    set Path(value: SliderPath);
    get Distance(): number;
    get Position(): Vector2;
    set Position(value: Vector2);
    LazyEndPosition?: Vector2;
    LazyTravelDistance: number;
    LazyTravelTime: number;
    private repeatCount;
    get RepeatCount(): number;
    set RepeatCount(value: number);
    get SpanDuration(): number;
    Velocity: number;
    TickDistance: number;
    TickDistanceMultiplier: number;
    private classicSliderBehaviour;
    get ClassicSliderBehaviour(): boolean;
    set ClassicSliderBehaviour(value: boolean);
    SliderVelocityMultiplier: number;
    HeadCircle: SliderHeadCircle;
    TailCircle: SliderTailCircle;
    LastRepeat: SliderRepeat;
    constructor(slider: Partial<Slider>);
    SpanCount(): number;
    CurvePositionAt(progress: number): Vector2;
    ProgressAt(progress: number): number;
    SpanAt(progress: number): number;
    protected ApplyDefaultsToSelf(beatmap: Beatmap): void;
    protected CreateNestedHitObjects(): void;
    updateNestedPositions(): void;
    protected CreateHitWindows(): HitWindows;
}
