import { HitResult } from "../HitResult";
import { DifficultyRange, HitWindows } from "../HitWindows";
export declare class EmptyHitWindows extends HitWindows {
    private static readonly ranges;
    IsHitResultAllowed(result: HitResult): boolean;
    protected GetRanges(): DifficultyRange[];
}
