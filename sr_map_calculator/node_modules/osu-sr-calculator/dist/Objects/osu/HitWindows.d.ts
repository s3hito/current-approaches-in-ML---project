import { HitResult } from "./HitResult";
export declare class DifficultyRange {
    readonly Result: HitResult;
    Min: number;
    Average: number;
    Max: number;
    constructor(result: HitResult, min: number, average: number, max: number);
}
export declare class HitWindows {
    private readonly base_ranges;
    private perfect;
    private great;
    private good;
    private ok;
    private meh;
    private miss;
    protected LowestSuccesfulHitResult(): HitResult;
    GetAllAvailableWindows(): {
        result: HitResult;
        length: number;
    }[];
    IsHitResultAllowed(result: HitResult): boolean;
    setDifficulty(difficulty: number): void;
    ResultFor(timeOffset: number): HitResult;
    WindowFor(result: HitResult): number;
    CanBeHit(timeOffset: number): boolean;
    protected GetRanges(): DifficultyRange[];
}
export declare const GetDifficultyRange: (difficulty: number, min: number, mid: number, max: number) => number;
export declare const GetDifficultyRangeSingle: (difficulty: number) => number;
