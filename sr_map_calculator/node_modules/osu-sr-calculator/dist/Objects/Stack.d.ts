export declare class Stack<T> {
    private _array;
    private _size;
    private _version;
    private DefaultCapacity;
    constructor(collection?: T[]);
    get Count(): number;
    Clear(): void;
    Contains(item: T): boolean;
    Pop(): T;
    Push(item: T): void;
}
