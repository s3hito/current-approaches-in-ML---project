"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PathControlPoint = void 0;
class PathControlPoint {
    get Position() { return this.position; }
    ;
    set Position(value) {
        if (!!this.position && value.equals(this.position))
            return;
        this.position = value;
    }
    ;
    get Type() { return this.type; }
    ;
    set Type(value) {
        if (value === this.type)
            return;
        this.type = value;
    }
    ;
    constructor(position, type = null) {
        this.Position = position;
        this.Type = type;
    }
    ;
    Equals(other) {
        return this.Position.equals(other.Position) && this.Type === other.Type;
    }
    ;
    ToString() {
        return !this.type
            ? `Position=${this.Position.x},${this.Position.y}`
            : `Position=${this.Position.x},${this.Position.y} Type=${this.Type}`;
    }
    ;
}
exports.PathControlPoint = PathControlPoint;
;
