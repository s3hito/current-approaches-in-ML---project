export declare class SliderEventGenerator {
    static TAIL_LENIENCY: number;
    static Generate(startTime: number, spanDuration: number, velocity: number, tickDistance: number, totalDistance: number, spanCount: number): SliderEventDescriptor[];
    private static generateTicks;
}
export declare enum SliderEventType {
    Tick = 0,
    LegacyLastTick = 1,
    Head = 2,
    Tail = 3,
    Repeat = 4
}
export declare class SliderEventDescriptor {
    Type: SliderEventType;
    Time: number;
    SpanIndex: number;
    SpanStartTime: number;
    PathProgress: number;
    constructor(type: SliderEventType, time: number, spanIndex: number, spanStartTime: number, pathProgress: number);
}
