"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.OsuService = void 0;
const axios_1 = __importDefault(require("axios"));
const fs_1 = __importDefault(require("fs"));
const crypto_1 = require("crypto");
class OsuService {
    async getOsuBeatmap(map_id) {
        const osuResponse = await axios_1.default.get(`https://osu.ppy.sh/osu/${map_id}`);
        if (osuResponse.status === 200) {
            return osuResponse.data;
        }
        else
            throw new Error(osuResponse.data);
    }
    ;
    async getOsuBeatmapFromFile(map_id, path, checksum) {
        try {
            if (!!checksum) {
                const md5 = await createMD5(path);
                if (!md5 || md5 !== checksum)
                    return await this.getOsuBeatmap(map_id);
            }
            const fileRes = fs_1.default.readFileSync(path).toString();
            return fileRes;
        }
        catch (error) {
            return await this.getOsuBeatmap(map_id);
        }
        ;
    }
    ;
}
exports.OsuService = OsuService;
;
const createMD5 = (filepath) => {
    return new Promise(resolve => {
        try {
            const hash = (0, crypto_1.createHash)("md5");
            const rStream = fs_1.default.createReadStream(filepath);
            rStream.on("data", data => {
                hash.update(data);
            });
            rStream.on("end", () => {
                resolve(hash.digest("hex"));
            });
            rStream.on("error", () => {
                resolve(null);
            });
        }
        catch (error) {
            resolve(null);
        }
        ;
    });
};
